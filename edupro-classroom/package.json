{
  "name": "edupro-classroom",
  "version": "2.0.0",
  "description": "EduPro WebRTC Classroom — MediaRecorder relay, no build step needed",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev":   "node server.js",
    "certs": "node scripts/gen-certs.js",
    "setup": "npm install && npm run certs"
  },
  "dependencies": {
    "express":   "^4.18.2",
    "socket.io": "^4.7.2",
    "qrcode":    "^1.5.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
